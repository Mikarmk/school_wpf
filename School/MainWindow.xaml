<Window x:Class="School.MainWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
        Title="Школа" Height="350" Width="500" MinWidth="300" MinHeight="200">
    <Grid>
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#FF0372FF" Offset="0.0"/>
                <GradientStop Color="#FF8D59F6" Offset="1.0"/>
            </LinearGradientBrush>
        </Grid.Background>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Школа" FontSize="24" FontWeight="Bold" Margin="10" HorizontalAlignment="Center" Foreground="White"/>

        <Button Content="Учителя" Click="TeachersButton_Click" Grid.Row="1" Margin="70, 10, 70, 10" BorderBrush="{x:Null}" Background="White"/>
        <Button Content="Ученики" Click="StudentsButton_Click" Grid.Row="2" Margin="70, 10, 70, 10" BorderBrush="{x:Null}" Background="White"/>
        <Button Content="Классы" Click="ClassesButton_Click" Grid.Row="3" Margin="70, 10, 70, 10" BorderBrush="{x:Null}" Background="White"/>

    </Grid>
</Window>